services:
  caddy:
    image: caddy:2.9
    restart: always
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - ./static.caddyfile:/etc/caddy/Caddyfile
      - ~/projects/caddy-static/content:/srv/content
      - caddy_data:/data

volumes:
  caddy_data:
